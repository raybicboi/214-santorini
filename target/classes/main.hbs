<!DOCTYPE html>
<html>

<head>
  <title>Santorini</title>
{{!--   added script tag --}}
{{!-- //   <script src="handlebars-v4.7.7.js"></script> --}}
   <style>
     #board {
       display: grid;
       grid-template-columns: auto auto auto auto auto;
       background-color: #2196F3;
       padding: 10px
     }

     .cell {
       background-color: rgba(255, 255, 255, 0.8);
       border: 1px solid rgba(0, 0, 0, 0.8);
       padding: 20px;
       font-size: 30px;
       height: 25vmin;
       text-align: center;
     }

     #bottombar {
       height: 5vh;
     }

     #instructions {
       height: 8vh;
       font-size: 2vw;
       background-color: white;
       padding: 0px;
       margin: 0px;
       text-align: center;
       border: 1px solid black;
     }

     #bottombar button {
       height: 100%;
       margin: 0;
       padding: 0;
       position: relative;
       text-align: center;
       font-size: 2vw;
     }

     html,
     body {
       height: 100%;
       margin: 0;
       padding: 0;
     }

     .playable { background-color: darkred; color: white;}

     .playable-player { background-color: lightblue;}
   </style>
</head>

<body>
  <div id="instructions">{{instructions}}</div>
  <div id="board">
    {{#each cells}}
      {{#if link}}
        <a href={{link}}><div class="cell {{clazz}}">{{text}}</div></a>
      {{else}}
        <div class="cell {{clazz}}">{{text}}</div>
      {{/if}}
    {{/each}}
  </div>
  <div id="bottombar">
    <a href="/newgame"><button>New Game</button></a>
    <a href="/base"><button>Base Logic</button></a>
    <a href="/athena"><button>Athena</button></a>
    <a href="/demeter"><button>Demeter</button></a>
    <a href="/minotaur"><button>Minotaur</button></a>
    <a href="/pan"><button>Pan</button></a>
  </div>

</body>

<!-- import the the tic.ts file generated by browserify -->
{{!-- <script src="tic.js"></script> --}}

</html>